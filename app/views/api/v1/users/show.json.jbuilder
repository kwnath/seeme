json.(@user, :id,
             :email,
             :nickname,
             :gender,
             :language,
             :avatar,
             :bio,
             :authentication_token,
             :created_at
     )
