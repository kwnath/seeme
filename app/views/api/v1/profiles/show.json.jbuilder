json.(@user, :id,
             :email,
             :nickname,
             :gender,
             :language,
             :avatar,
             :authentication_token,
             :created_at,
             :tag_list,
             :bio,
             :latitude,
             :longitude
     )
